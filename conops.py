operationslu = {
    "in.": {
        "cm.": lambda x: x * 2.54,
        "ft.": lambda x: x / 12,
        "yards": lambda x: x / 36,
        "m.": lambda x: x * (127 / 5000),
        "miles": lambda x: x / 63360,
        "km": lambda x: x * (25 / 984252),
        "in.": lambda x: x,
    },
    "cm.": {
        "m.": lambda x: x / 100,
        "in.": lambda x: x / 2.54,
        "ft.": lambda x: x * (25 / 762),
        "yards": lambda x: x * (25 / 2286),
        "miles": lambda x: x * (5 / 804672),
        "km": lambda x: x / 100_000,
        "cm.": lambda x: x,
    },
    "m.": {
        "km": lambda x: x / 1000,
        "cm.": lambda x: x * 100,
        "ft.": lambda x: x / 0.3048,
        "yards": lambda x: x / 0.9144,
        "in.": lambda x: x * (5000 / 127),
        "miles": lambda x: x * (125 / 201168),
        "m.": lambda x: x,
    },
    "ft.": {
        "m.": lambda x: x * 0.3048,
        "in.": lambda x: x * 12,
        "cm.": lambda x: x * (762 / 25),
        "yards": lambda x: x / 3,
        "miles": lambda x: x / 5280,
        "km": lambda x: x * (381 / 1250000),
        "ft.": lambda x: x,
    },
    "yards": {
        "m.": lambda x: x * 0.9144,
        "in.": lambda x: x * 36,
        "cm.": lambda x: x * (2286 / 25),
        "ft.": lambda x: x * 3,
        "miles": lambda x: x / 1760,
        "km": lambda x: x * (1143 / 1250000),
        "yards": lambda x: x,
    },
    "miles": {
        "km": lambda x: x * 1.609344,
        "in.": lambda x: x * 63360,
        "cm.": lambda x: x * (804672 / 5),
        "ft.": lambda x: x * 5280,
        "yards": lambda x: x * 1760,
        "miles": lambda x: x,
    },
    "km": {
        "m.": lambda x: x * 1000,
        "miles": lambda x: x / 1.609344,
        "cm.": lambda x: x * 100_000,
        "in.": lambda x: x * (5000000 / 127),
        "ft.": lambda x: x * (1250000 / 381),
        "yards": lambda x: x * (1250000 / 1143),
        "km": lambda x: x,
    },
}

operationstu = {
    "°C": {
        "°F": lambda x: (x * (9 / 5)) + 32,
        "K": lambda x: x + 273.15,
        "°C": lambda x: x,
    },
    "°F": {
        "°C": lambda x: (x - 32) * (5 / 9),
        "K": lambda x: ((x - 32) * (5 / 9)) + 273.15,
        "°F": lambda x: x,
    },
    "K": {
        "°C": lambda x: x - 273.15,
        "°F": lambda x: ((x - 273.15) * (9 / 5)) + 32,
        "K": lambda x: x,
    },
}

operationsmu = {
    'kg.': {
        'g.': lambda x: x * 1000,
        'lb.': lambda x: x * 2.20462,
        'oz.': lambda x: x * 35.274,
        't': lambda x: x / 1000,
        'mg': lambda x: x * 1000000,
        'st': lambda x: x * 0.15747,
        'kg.': lambda x: x,
    },
    'g.': {
        'kg.': lambda x: x / 1000,
        'lb.': lambda x: x * 0.00220462,
        'oz.': lambda x: x * 0.035274,
        't': lambda x: x / 1000000,
        'mg': lambda x: x * 1000,
        'st': lambda x: x * 0.00015747,
        'g.': lambda x: x,
    },
    'lb.': {
        'kg.': lambda x: x * 0.453592,
        'g.': lambda x: x * 453.592,
        'oz.': lambda x: x * 16,
        't': lambda x: x * 0.000453592,
        'mg': lambda x: x * 453592,
        'st': lambda x: x / 14,
        'lb.': lambda x: x,
    },
    'oz.': {
        'kg.': lambda x: x * 0.0283495,
        'g.': lambda x: x * 28.3495,
        'lb.': lambda x: x / 16,
        't': lambda x: x * 0.0000283495,
        'mg': lambda x: x * 28349.5,
        'st': lambda x: x / 224,
        'oz.': lambda x: x,
    },
    't': {
        'kg.': lambda x: x * 1000,
        'g.': lambda x: x * 1000000,
        'lb.': lambda x: x * 2204.62,
        'oz.': lambda x: x * 35274,
        'mg': lambda x: x * 1000000000,
        'st': lambda x: x * 157.473,
        't': lambda x: x,
    },
    'mg': {
        'kg.': lambda x: x / 1000000,
        'g.': lambda x: x / 1000,
        'lb.': lambda x: x * 0.00000220462,
        'oz.': lambda x: x * 0.000035274,
        't': lambda x: x / 1000000000,
        'st': lambda x: x * 0.00000015747,
        'mg': lambda x: x,
    },
    'st': {
        'kg.': lambda x: x * 6.35029,
        'g.': lambda x: x * 6350.29,
        'lb.': lambda x: x * 14,
        'oz.': lambda x: x * 224,
        't': lambda x: x * 0.00635029,
        'mg': lambda x: x * 6350290,
        'st': lambda x: x,
    },
}
operationsdu = {
    'b': {
        'B': lambda x: x / 8,
        'KB': lambda x: x / (1024 * 8),
        'MB': lambda x: x / (1024**2 * 8),
        'GB': lambda x: x / (1024**3 * 8),
        'TB': lambda x: x / (1024**4 * 8),
        'b': lambda x: x,
    },
    'B': {
        'b': lambda x: x * 8,
        'KB': lambda x: x / 1024,
        'MB': lambda x: x / 1024**2,
        'GB': lambda x: x / 1024**3,
        'TB': lambda x: x / 1024**4,
        'B': lambda x: x,
    },
    'KB': {
        'b': lambda x: x * 1024 * 8,
        'B': lambda x: x * 1024,
        'MB': lambda x: x / 1024,
        'GB': lambda x: x / 1024**2,
        'TB': lambda x: x / 1024**3,
        'KB': lambda x: x,
    },
    'MB': {
        'b': lambda x: x * 1024**2 * 8,
        'B': lambda x: x * 1024**2,
        'KB': lambda x: x * 1024,
        'GB': lambda x: x / 1024,
        'TB': lambda x: x / 1024**2,
        'MB': lambda x: x,
    },
    'GB': {
        'b': lambda x: x * 1024**3 * 8,
        'B': lambda x: x * 1024**3,
        'KB': lambda x: x * 1024**2,
        'MB': lambda x: x * 1024,
        'TB': lambda x: x / 1024,
        'GB': lambda x: x,
    },
    'TB': {
        'b': lambda x: x * 1024**4 * 8,
        'B': lambda x: x * 1024**4,
        'KB': lambda x: x * 1024**3,
        'MB': lambda x: x * 1024**2,
        'GB': lambda x: x * 1024,
        'TB': lambda x: x,
    },
}